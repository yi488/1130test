# 项目结构文档

## 📁 目录结构

```
cultural-heritage-app/
├── data/                          # 数据存储
│   └── cultural_heritage.sqlite   # SQLite数据库文件
├── docs/                          # 项目文档
│   ├── PROJECT_STRUCTURE.md       # 项目结构说明
│   └── API.md                     # API文档
├── public/                        # 静态资源
│   ├── images/                    # 文物图片
│   │   ├── bronze_ding.jpg        # 青铜饕餮纹方鼎
│   │   ├── blue_white_vase.jpg    # 青花缠枝莲纹梅瓶
│   │   ├── jade_bi.jpg            # 和田玉雕龙纹璧
│   │   └── ...                    # 其他文物图片
│   └── 3DModels/                  # 3D模型文件
├── src/                           # 前端React代码
│   ├── components/                # React组件
│   │   ├── artifacts/             # 文物相关组件
│   │   ├── layout/                # 布局组件
│   │   └── ui/                    # UI组件
│   ├── pages/                     # 页面组件
│   ├── hooks/                     # 自定义Hooks
│   ├── types/                     # TypeScript类型定义
│   └── router/                    # 路由配置
├── src-tauri/                     # 后端Rust代码
│   ├── src/
│   │   ├── api/                   # API接口
│   │   ├── db/                    # 数据库相关
│   │   │   ├── models/            # 数据模型
│   │   │   └── migrations/        # 数据库迁移
│   │   └── main.rs                # 主程序入口
│   └── Cargo.toml                 # Rust依赖配置
├── package.json                   # Node.js依赖配置
├── vite.config.ts                 # Vite配置
└── README.md                      # 项目说明
```

## 🎯 核心模块说明

### 前端 (React + TypeScript)
- **components/artifacts/**: 文物展示相关组件
- **pages/**: 页面级组件
- **hooks/**: 数据获取和状态管理
- **types/**: TypeScript类型定义

### 后端 (Rust + Tauri)
- **api/**: RESTful API接口
- **db/**: 数据库操作和模型
- **migrations/**: 数据库版本控制

### 数据存储
- **SQLite**: 文物元数据存储
- **文件系统**: 图片和3D模型存储

## 🔧 开发规范

### 文件命名
- **组件**: PascalCase (如 `ArtifactCard.tsx`)
- **图片**: snake_case (如 `bronze_ding.jpg`)
- **数据库**: snake_case (如 `cultural_heritage.sqlite`)

### 目录组织
- 按功能模块组织代码
- 分离前端和后端代码
- 静态资源统一管理

### 数据流
```
前端组件 → API调用 → Rust后端 → SQLite数据库
    ↓           ↓          ↓           ↓
   UI显示 ← JSON响应 ← 数据查询 ← 文件存储
```
